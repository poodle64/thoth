# ============================================================================
# Operating System
# ============================================================================
.DS_Store
._.DS_Store
._*
.AppleDouble
.LSOverride
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini

# ============================================================================
# Editor / IDE
# ============================================================================
.idea/
!.vscode/
.vscode/*
!.vscode/settings.json
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/extensions.json
!.vscode/*.code-snippets
*.swp
*.swo
*~

# ============================================================================
# Claude Code
# ============================================================================
.claude/settings.local.json
.claude/CLAUDE.local.md
.claude/.sessions/
.claude/worktrees/
.claude/agents/
.claude/scheduled_tasks.lock

# Symlinks into the household Claude harness, which lives in the sibling
# nephalemsec/master-project checkout (~/Desktop/Github/master-project).
#
# DELIBERATE DEVIATION from master rule git/22-ignore-files.md ("Master Symlinks
# (Always Tracked)"), which mandates tracking these two and states the deviation
# is not warranted by repo visibility. Overridden for thoth by maintainer
# decision: this repo is public, and a tracked symlink that resolves only on a
# machine with master-project as a sibling gives every other clone a dangling
# link. Revisit if that rule is amended.
.claude/rules/master
docs/master

# Legacy harness symlinks, superseded by the mimir plugin (see the master
# setup-symlinks command). Ignored so they cannot be re-committed if a tool
# recreates them.
.claude/commands/master
.claude/hooks/master
.claude/skills

# ============================================================================
# Environment & Secrets
# ============================================================================
.env
.env.*
!.env.example
.envrc.local
.direnv/

# ============================================================================
# Python
# ============================================================================
__pycache__/
*.py[cod]
*.so
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# ============================================================================
# Node.js
# ============================================================================
node_modules/
.npm/
.pnpm-store/
.svelte-kit/
npm-debug.log*

# ============================================================================
# Testing / MCP Tools
# ============================================================================
.playwright-mcp/
phase*.png

# ============================================================================
# TikZ / LaTeX Diagrams
# ============================================================================
docs/diagrams/*.aux
docs/diagrams/*.log
docs/diagrams/*.pdf

# ============================================================================
# Logs
# ============================================================================
logs/
*.log

# ============================================================================
# Rust / Tauri
# ============================================================================
target/
src-tauri/target/
*.dylib

# Tauri build-generated schemas (regenerated by tauri-build on every build)
src-tauri/gen/schemas/

# ============================================================================
# Private planning docs (not for public repos)
# ============================================================================
docs/product/

# ============================================================================
# Project-Specific
# ============================================================================
# Models (too large for git)
models/

# Internal docs (local dev only, not for public repo)
docs/deployment/
docs/research/
# Branding: track hero image and overview screenshot, gitignore private design work
docs/branding/colour-schemes.md
docs/branding/colour-preview.html
docs/branding/icon-design-brief.md
docs/branding/app-icon-prompt.md

# Temporary files
*.tmp
*.temp
.mcp.json

# ============================================================================
# Testing / Playwright
# ============================================================================
test-results/
playwright-report/
blob-report/
.playwright-mcp/

# Extracted by CI (nix-check.yaml) to run the base branch copy of the
# pnpmDeps hash script; never committed.
scripts/.ci-update-pnpm-deps-hash.sh
