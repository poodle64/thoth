# @poodle64 packages (design-tokens, ui) publish to GitHub Packages.
# Auth is supplied by the environment, never by this file: locally from
# ~/.npmrc, in CI by setup-node's registry-url + NODE_AUTH_TOKEN.
@poodle64:registry=https://npm.pkg.github.com
