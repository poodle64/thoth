overrides:
  nanoid@<3.3.17: '>=3.3.17'
  postcss@<8.5.18: '>=8.5.18'
  # CVE-2026-16221 (policy bypass) and CVE-2026-18446 (host confusion), both
  # HIGH. Transitive via ajv, reached twice: @poodle64/ui -> @jsonforms/core
  # and stylelint -> table. Neither dependant pins it, so the floor lifts both.
  # Capped below 4 because ajv@8 declares ^3.0.1: an unbounded floor resolved to
  # 4.1.3, which is patched but outside the range its dependant asked for.
  fast-uri@<3.1.5: '>=3.1.5 <4'
